describe('shopFilters', function() {

    beforeEach(module('ecoposApp'));

    it('should ...', inject(function($filter) {

        var filter = $filter('shopFilters');

        expect(filter('input')).toEqual('output');

    }));

});